# Copy this file to ".env" (same folder) and fill in your real key(s).
# GROQ_API_KEY is optional here if you've already used the main app —
# the script also checks the main app's .env.local automatically.
GROQ_API_KEY=
GEMINI_API_KEY=
